{
  "name": "codex-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && chmod +x ./node_modules/.bin/vite && npx vite build",
    "start": "cd backend && npm start"
  },
  "dependencies": {
    "@monaco-editor/react": "^4.6.0",
    "axios": "^1.4.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.14.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^5.4.21"
  }
}
